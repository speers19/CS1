#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
int n;
int count = 0;
double ss;
double sss;
double s =
double r;
double z;
double pi = 2/z;

printf("Please enter a positive integer\n");
scanf("%d",&n);

ss=0;

while (count<n)
{
count++;
r = ((sqrt(2))/2);
}

if (count>n)
{
printf("That's not a positive integer!");
}
else
{
printf("Your approximation of pi is %lf\n",pi);
}
}
